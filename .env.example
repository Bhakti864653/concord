# Copy this file to ".env" and fill in real values there.
# Never commit the real ".env" file — it's already excluded via .gitignore.

SUPABASE_URL=your-project-url-here
SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here
SUPABASE_SECRET_KEY=your-service-role-key-here

# The one account allowed to trigger a matching run (checked by email, no
# separate admin role/table exists).
ADMIN_EMAIL=your-email-here

# Comma-separated list of origins allowed to call this API (CORS). Defaults
# to http://localhost:3000 if unset - add your deployed frontend's URL here
# in production.
FRONTEND_ORIGINS=http://localhost:3000

# Backend-only - powers the AI-written match narrative
# (backend/app/ai_explanation.py). Never exposed to the frontend, never
# logged, never returned in any API response.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
